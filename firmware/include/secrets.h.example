// Copy this file to `include/secrets.h` (gitignored) and fill in your values.
#pragma once

#define WIFI_SSID  "your-wifi-network"
#define WIFI_PASS  "your-wifi-password"

// Upload destination — your homelab's /api/upload endpoint. Use the LAN IP or
// a stable hostname; mDNS (.local) works on most home networks.
#define UPLOAD_URL "http://192.168.1.50:8080/api/upload"
